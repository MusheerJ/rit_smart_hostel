import 'package:flutter/material.dart';

class ComplaintRegScreen extends StatelessWidget {
  const ComplaintRegScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
